[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "screen-delta-md"
version = "0.1.0"
description = "Desktop screen change monitor scaffold"
requires-python = ">=3.11"
