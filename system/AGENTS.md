# Factory Manager Protocol

- Codex is the FACTORY MANAGER.
- Before any task: run `tools/verify.sh`.
- After any task: run `tools/tidy.sh` and then `tools/verify.sh`.
- Never create new top-level folders.
- Operate only inside `workspace/`, `inbox/`, `runtime/`, and `cache/`.
