# Handoff (2026-02-16)

## 今日やったこと
- `evolution_eval/run_logger.py`
  - `exit_code==0` を `SUCCESS` として扱うように調整。
  - `repair_attempted` / `repair_success` を記録。
- `evolution_eval/analyze.py`
  - run_loggerのJSONLのみを真実源として集計する方針に整理。
  - injected subset 指標を保持。
  - Cohen's h と解釈（negligible/small/medium/large）を追加。
- `evolution_eval/kernel_runner.sh`
  - 依存修復フローを導入（`repair_package_json.py` 利用）。
  - 現在は修復トリガ判定を run_logger classify に寄せる方向で改修途中。
- `evolution_eval/repair_package_json.py`
  - `broken-dep` 削除、`scripts.test` 修復を実装。
- テスト
  - `test_analyze.py` を追加（Cohen's h/解釈）。
  - 既存 `test_analyze_metrics.py` はローカル未配置で `unittest` 実行時に ImportError。

## 現状の課題
1. `kernel_runner.sh` の修復判定ロジックを最終確定する必要あり。
   - 要件: heuristic(trace/stderr) ではなく、artifact→run_logger.classify結果（FAIL + DEPENDENCY_ERROR）でのみ repair。
2. 統合テストの固定化。
   - `test_kernel_runner_integration.sh` / `test_kernel_repair.sh` をリポジトリに正式配置して定期実行。
3. 比較実験の成立確認。
   - baseline/kernel の injected subset で改善差が出るかを再実行で確認。

## 明日の最優先
1. `kernel_runner.sh` の修復トリガ条件を最終版へ整理。
2. 5-run baseline + 5-run kernel 実行。
3. `python3 evolution_eval/analyze.py` で以下確認:
   - `baseline.failure_rate`
   - `kernel.failure_rate`
   - `improvement.injected_absolute_improvement`
   - `effect_size.overall / injected`
4. テスト一式の実行:
   - `python3 -m unittest test_analyze.py`
   - （配置後）`python3 -m unittest test_analyze_metrics.py`

## 主要ファイル
- `evolution_eval/run_logger.py`
- `evolution_eval/analyze.py`
- `evolution_eval/kernel_runner.sh`
- `evolution_eval/repair_package_json.py`
- `test_analyze.py`

## 補足
- 今日時点では、評価精度の中心は「run_loggerのstatus/reason_codeを唯一の判定源」に寄せる方針。
- 解析側での推論（heuristic failure inference）は避ける。
